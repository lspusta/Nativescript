android {
            defaultConfig {
                applicationId = "org.nativescript.Lucas1"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }